# SSR dependency notes
#
# Recommended: uv install (CUDA 12.8 driver, PyTorch cu128):
#   ./install_env_cu128.sh
# Or:
#   uv sync --extra data
#
# Data prep only (prepare_msmarco.py):
#   uv sync --extra data
#   # or pip: pip install -r requirements-data.txt
#
# Training deps are managed in pyproject.toml; do not pip install -r requirements.txt for training,
# or you may get cu130 PyTorch and errors on CUDA 12.8 drivers.
#
# Files:
#   requirements-data.txt  - Data prep (prepare_msmarco.py)
#   requirements-train.txt - Training pip fallback (install cu128 torch first)
#   pyproject.toml         - uv config (torch cu128 index)

-r requirements-data.txt
